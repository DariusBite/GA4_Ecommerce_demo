<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ga4ecommercedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GA4EcommerceDemo">
        <activity android:name=".Purchase"></activity>
        <activity android:name=".Checkout3" />
        <activity android:name=".Checkout2" />
        <activity android:name=".Checkout" />
        <activity android:name=".ViewCart" />
        <activity android:name=".ViewProductDetails" />
        <activity android:name=".ViewItemList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>